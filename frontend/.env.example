# Frontend (Vite) environment variables
# Copy to .env.local (recommended) and set values.

# Backend API base URL (no trailing slash recommended)
# Example: http://127.0.0.1:8000
VITE_API_BASE_URL=

# Optional: preseed an OAuth access token for local testing.
# NOTE: Do not commit real tokens.
VITE_OAUTH_ACCESS_TOKEN=
